# Cloudflare Workers Builds (static admin panel)
# Deploy: npx wrangler deploy --config apps/admin-panel/wrangler.toml

name = "soatbay-admin"
compatibility_date = "2026-01-01"

[assets]
directory = "./dist/admin-panel/browser"
